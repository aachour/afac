I need you to update my Laravel Livewire component and Blade view.

Important clarification:

I do not have dynamic columns. I have 3 fixed separate columns, and each column must be edited independently.

The fields are:

Column 1:

* col1
* col1_link
* col1_arabic
* col1_arabic_link

Column 2:

* col2
* col2_link
* col2_arabic
* col2_arabic_link

Column 3:

* col3
* col3_link
* col3_arabic
* col3_arabic_link

Each column should have its own Add/Edit button beside it.

When clicking the button for column 1, only the column 1 fields should be loaded and updated.

When clicking the button for column 2, only the column 2 fields should be loaded and updated.

When clicking the button for column 3, only the column 3 fields should be loaded and updated.

Use one shared modal, but the modal content should change depending on which column is being edited.

The modal should allow editing:

* English column text
* Arabic column text
* English links
* Arabic links

Important note about links:

The link fields are JSON fields, not plain strings.

These fields are JSON:

* col1_link
* col2_link
* col3_link
* col1_arabic_link
* col2_arabic_link
* col3_arabic_link

Each JSON field can contain multiple links, not only one link.

Each link item should contain:

* title
* link

The user should be able to:

* Add a new English link row
* Remove an English link row
* Edit each English link title
* Edit each English link URL
* Add a new Arabic link row
* Remove an Arabic link row
* Edit each Arabic link title
* Edit each Arabic link URL

Please make sure the Livewire component handles these JSON fields correctly, whether the current value is already an array, a JSON string, empty, or null.

Also prepare the edit functionality in the Livewire component.

Required behavior:

* Add an Add/Edit button near each of the 3 columns.
* Clicking the button opens the modal.
* The modal loads the related column data.
* The modal shows repeatable rows for English links and Arabic links.
* The user can add and remove link rows inside the modal.
* Saving updates only the selected column fields.
* Saving should store the link fields as JSON-compatible arrays.
* Closing or saving the modal should reset the temporary form data.
* Do not convert the fixed fields into a dynamic columns array.
* Do not change the existing structure unless necessary.
* Keep the implementation compatible with the current component logic.
* Return the full updated Blade file and full updated Livewire component file.
